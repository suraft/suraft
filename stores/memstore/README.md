# suraft-memstore

This is an in-memory example `RaftLogStorage` and `RaftStateMachine` implementation based on [suraft](https://github.com/suraft/suraft/).

This crate is built mainly for testing or demonstrating purpose.:)
